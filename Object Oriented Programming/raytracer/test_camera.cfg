camera = {
    resolution = {
        width = 800;
        height = 600;
    };
    position = {
        x = 0;
        y = 10;  # Position élevée de la caméra
        z = 20;  # Reculée pour voir la scène
    };
    fieldOfView = 90.0;
    lookAt = {
        x = 0;
        y = 0;  # Regard vers le centre
        z = 0;
    };
};

lights = (
    {
        type = 1;  # Ambient
        intensity = 0.2;
    },
    {
        type = 2;  # Directional
        intensity = 0.8;
        direction = {
            x = 0.5;
            y = -1.0;
            z = -0.5;
        };
    }
);

renderer = {
    type = 1;  # Basic renderer
}; 